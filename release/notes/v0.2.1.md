# 瓦刻（VALOFRAME）v0.2.1

v0.2.1 是自 v0.1.0-beta.1 以来的一次重要桌面版升级。本轮重点完善了多来源接入、快速挑片、扫描可靠性、目录恢复和本地数据安全。本 Release 实际分发的是由发布负责人在本机离线构建的精简安装包（约 8.17 MB）；与早期计划的稳定流水线产物的差异见文末“实际发布与早期计划”。

## 下载与校验

- **安装包**：`VALOFRAME-v0.2.1-x64-setup.exe`
- **大小**：8,564,783 字节（≈8.17 MB）
- **SHA-256**：`3e4cda3a6c5e5162195f59dd65b6968f7682eb5067f9824d7dcc93187ea7d3f0`

```powershell
Get-FileHash -Algorithm SHA256 -LiteralPath '.\VALOFRAME-v0.2.1-x64-setup.exe'
```

同一 Release 提供 `SHA256SUMS.txt`；安装前请逐项核对文件名与 SHA-256。

## 主要更新

### 更多本地录像来源

- 支持注册无畏时刻、NVIDIA App/ShadowPlay、Valorant Tracker 和普通 MP4 目录。
- NVIDIA、Tracker 与普通目录只在用户授权根目录内递归扫描 MP4；不读取 Tracker 私有数据库或 API，不跟随符号链接、junction 或其他 reparse point。
- H.264 视频可建立索引并由随包 FFmpeg 生成缩略图。HEVC/AV1 MP4 仍可被索引和保留；本版随包最小 FFmpeg 不提供 HEVC/AV1 缩略图（显示占位封面），内嵌播放不可用时可交给系统默认播放器，最终能否播放取决于系统解码能力。

### 独立的快速挑片工作流

- 快速挑片拥有独立范围，不再继承或修改素材库当前筛选；可按账号、英雄、地图、模式、日期、视频类型、标签和搜索词收窄候选素材。
- 支持入选、待定、跳过、撤销、保存进度后继续，以及提前结束并查看结果。
- 详情预览与快速挑片均支持全屏播放：可使用全屏按钮、`F` 或双击视频进入，使用 `Esc` 退出。
- 预览继续支持 `Space` / `K` 播放暂停、方向键与 `J` / `L` 快进快退、`M` 静音；输入框、下拉菜单和对话框聚焦时不会误触快捷键。

### 更可靠的扫描反馈

- 扫描页会显示每个启用来源是“今天扫描”“N 天未扫描”还是“尚未完成首次扫描”。从未完整扫描或达到 7 个本地自然日未扫描时会显示提醒。
- 只有完整成功的扫描才更新来源新鲜度；`partial`、`cancelled` 和 `failed` 不会伪装成成功扫描。
- 每个扫描任务终态会按任务 ID 去重并准确显示新增数量，包括“新增 0 个视频”；无法恢复摘要时会明确显示数量不可用。
- 新增“启动时自动扫描”设置，默认关闭。开启后只同步已加入自动同步的来源，短时间内重新启动不会重复扫描，手动同步始终可用。

### 来源恢复与安全重连

- 同一已注册来源内的文件或子目录改名，会优先按规范化路径、唯一 Windows 稳定文件身份，再按无歧义的旧库指纹安全重连。
- 可信重连会保留 clip ID、收藏、标签、备注、评审决定和结构化元数据；复制、硬链接或歧义候选不会被猜测合并。
- 来源根移动后可先预览再明确提交新位置；提交会重新验证授权边界、来源重叠、回收素材、删除 intent 和关键任务互斥。
- missing 或来源不可用的普通素材支持单条和批量“仅移除索引”，不会删除、移动或修改磁盘视频。

### 视频类型与事件时间轴修正

- 三杀、四杀、五杀、六杀、击杀集锦和死亡时刻会解析为唯一规范类别，并显示在素材卡片上。
- 修复击杀集锦因事件数量而被误判为普通多杀，以及英文单词或中文子串造成的类型误判。
- 普通多杀继续使用“分段起点 + 事件相对时间”；击杀/死亡集锦使用视频绝对时间，不再重复叠加分段起点。
- 击杀类高光只显示本人击杀并使用红色准星；死亡集锦只显示本人死亡并使用紫色骷髅。超出视频时长的事件不会被裁剪或渲染。

### 大素材库与数据升级

- 大素材库采用后端分页和分层虚拟化；批量整理操作使用单事务，并同步当前页、详情与筛选统计。
- 修复移入回收站或执行等价查询刷新后，素材库滚动位置跳回顶部的问题。
- 数据库目标版本升级为 schema v16。v13、v14 和 v15 数据库会在迁移前创建 SQLite Online Backup，并在单事务中升级。
- 迁移保留来源、clip ID、收藏、标签、备注、评审、缩略图、回收快照、永久删除 intent 和原始元数据。

## 安装与升级

- 支持 Windows 10/11 x64，安装器为当前用户安装，不需要机器级管理员安装范围。
- v0.1.0-beta.1 不包含稳定更新配置，无法应用内升级到本版本；旧用户需要手动安装 v0.2.1 一次。
- 本发布未注入更新公钥，不提供应用内更新；后续版本需从 Releases 页手动下载安装。
- 目标电脑缺少 Microsoft Edge WebView2 Runtime 时，安装过程需要联网下载运行时。
- Windows Authenticode 与 Tauri 更新签名是两套不同机制。当前安装器未使用 Authenticode，可能显示“未知发布者”或 SmartScreen 提示，请只从项目 Release 下载并核对 `SHA256SUMS.txt`。

## 数据与隐私

- 瓦刻保持本地优先，不提供云同步或遥测。素材、索引和用户整理数据留在本机。
- 扫描、重连、根目录恢复和“仅移除索引”不会修改来源视频；只有回收站中经过二次确认且仍满足身份校验的素材可以进入永久删除流程。
- 扫描、永久删除、视频导出、来源根重定位与更新安装由同一后端关键任务门禁互斥，失败或取消不会替换当前安装或放宽文件权限。

## 实际发布与早期计划

- 本 Release 实际分发的是发布负责人在本机离线构建的精简安装包（8.17 MB），构建自 `v0.2.1` 标签指向的 commit `74f85e992f52b71a940b36b106f343b13aea4185`，未改动任何源码。
- 构建门禁：前端 220 个 Node 测试 + 300 个 UI 测试全部通过；`tsc && vite build` 与 42 张固定游戏素材逐字节校验通过；`cargo fmt --check` 通过；`cargo build --release --locked --offline` 成功。
- 未注入更新公钥：本构建不含应用内更新，也未生成 Tauri/Minisign 签名；后续如恢复稳定更新通道，更新包仍必须通过 Tauri/Minisign 签名验证，且不允许安装 prerelease 或降级版本。
- 随包 FFmpeg：从固定源码提交 `ce3c09c101c8` 构建的最小静态版本（2,930,688 字节，SHA-256 `7703c7ec006d0a8a0b57dd43b76fe624639aaf9771aaa6ad30aeed94aa2472a5`，LGPL-3.0-or-later，零外部库），仅启用 H.264 缩略图能力（早期计划中的 HEVC 软件解码未包含）；该二进制已通过 Windows PE import 与合成素材缩略图烟测。
- 安装包内容已复核：主程序 + `bin\ffmpeg.exe`（最小版）+ 许可证与第三方合规材料，无 112 MB 全量 FFmpeg 组件。

## 个人非商业社区版说明

- 本版本由个人开发者以免费社区项目方式发布，是非官方工具，与 Riot Games、腾讯及 FFmpeg 项目不存在隶属、赞助或认可关系。“非商业”描述发布目的，不会限制项目第一方 MIT 代码，也不会免除第三方许可证义务。
- 安装包内置由固定源码提交构建、零外部库的最小 FFmpeg，仅作为独立命令行程序生成本地缩略图，许可证为 LGPL-3.0-or-later。FFmpeg 的构建证据、许可证、IJG 声明和精确对应源码保留在仓库发布材料中；这些技术材料不构成独立法律意见。
- Windows 安装器当前可能显示“未知发布者”或 SmartScreen 提示；应用内更新不可用，后续版本需手动安装。请只从本项目 Release 下载，并使用 `SHA256SUMS.txt` 核对除校验清单自身外的所有文件。

## 已知限制

- 当前仅支持 MP4，不支持 MKV、实时目录监听、云同步、自动转码、静默安装或 Beta 更新通道。
- 某些 HEVC/AV1 文件可能无法在 WebView2 中直接播放。本发布只为 H.264 生成缩略图；HEVC/AV1 只保留索引并提供系统播放器入口，实际播放取决于系统解码能力。
- Windows 稳定文件身份是最佳努力能力；跨来源或跨卷移动不会自动判断为同一素材，来源根移动需要用户明确选择并确认。
- 对仍可访问且不符合“仅移除索引”资格的素材，原有回收站与永久删除边界保持不变。

